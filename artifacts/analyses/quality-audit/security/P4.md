### Summary
No critical or high-severity issues found. Five low-severity (P3) findings: direct `process.env` access in GDPR controller, destructive purge endpoint on a public route, GDPR export sub-query limit risking incomplete data, unredacted audit log JSONB fields, and missing audit logging for consent/GDPR/purge operations.

### Findings
| severity | file | line | description |
|---|---|---|---|
| P3 | apps/api/src/gdpr/gdpr.controller.ts | 25 | `process.env.APP_NAME` accessed directly instead of injected `ConfigService` — bypasses env validation and contradicts project pattern |
| P3 | apps/api/src/purge/purge.controller.ts | 16 | Destructive cron endpoint (`POST /api/internal/purge`) exposed on a public route with `@AllowAnonymous()` — increases attack surface despite `CRON_SECRET` gating |
| P3 | apps/api/src/gdpr/gdpr.service.ts | 14 | `EXPORT_QUERY_LIMIT = 10_000` caps sessions, accounts, and consent sub-queries — a user with >10,000 records receives a truncated GDPR export |
| P3 | apps/api/src/audit/audit.repository.ts | 20 | `AuditLogEntry.before/after/metadata` accept `Record<string, unknown>` with no redaction — passwords, tokens, or other sensitive data may be persisted to `audit_logs` as-is |
| P3 | apps/api/src/consent/consent.controller.ts | 40 | Consent save operation is not logged to `AuditService` — no audit trail for consent changes |
| P3 | apps/api/src/gdpr/gdpr.controller.ts | 13 | GDPR export operation is not logged to `AuditService` — no audit trail for data portability requests |
| P3 | apps/api/src/purge/purge.controller.ts | 16 | Purge operation is not logged to `AuditService` — no audit trail for destructive anonymization runs |

### Metrics
- files_audited: 29
- issues_found: 7
- P0: 0 | P1: 0 | P2: 0 | P3: 7

### Recommendations (prioritized, with effort estimate)
1. **Replace `process.env.APP_NAME` with `ConfigService`** in `gdpr.controller.ts` (5 min)
2. **Move purge endpoint to a non-public route** or restrict by IP/range in addition to `CRON_SECRET` (1-2h)
3. **Add audit logging** to consent save, GDPR export, and purge controllers via injected `AuditService` (30 min)
4. **Add redaction helper** for `AuditLogEntry.before/after` to strip known sensitive keys before writing (e.g., `password`, `accessToken`, `refreshToken`) (2h)
5. **Implement pagination or streaming** for GDPR export sub-queries to remove the 10,000-row cap (2-4h)
