### Summary
Partition P2 (Users, orgs, multi-tenancy) contains 2 TODO markers and several hardcoded strings/roles that undermine tenant isolation correctness. No console.log, deprecated APIs, unused imports, or commented-out code were found.

### Findings
| severity | file | line | description |
|----------|------|------|-------------|
| P1 | apps/api/src/tenant/tenant.interceptor.ts | 113 | TODO(Phase 3, #389): Parent tenant resolution is formally deferred. `parentOrganizationId` exists in schema but resolution is not implemented, so `tenantId` may equal `orgId` rather than the true parent tenant. Breaks isolation for child orgs. |
| P1 | apps/api/src/tenant/tenant.interceptor.ts | 76 | Hardcoded silent fallback: when `db` is null, `tenantId` is set to `activeOrganizationId` without validation. Fails open on missing DB connection. |
| P1 | apps/api/src/tenant/tenant.interceptor.ts | 106 | Hardcoded silent fallback: when `lookupOrganization` returns null, `tenantId` falls back to `orgId` instead of throwing. |
| P1 | apps/api/src/tenant/tenant.interceptor.ts | 122 | Hardcoded silent fallback: `catch` block logs the error and returns `orgId`, suppressing DB failures during tenant resolution. |
| P1 | apps/api/src/user/user.controller.ts | 123 | Hardcoded cookie name `better-auth.session_token` in `clearCookie`. Couples user module to auth library implementation; cookie rename breaks logout. |
| P2 | apps/api/src/user/repositories/drizzleUserPurge.repository.ts | 81 | TODO: Optimize `purgeOwnedOrganizations` sequential loop with `inArray()` batch operations. |
| P2 | apps/api/src/user/repositories/drizzleUserPurge.repository.ts | 40 | Hardcoded anonymized email domain `@anonymized.local`. Should be configurable or extracted to a constant. |
| P2 | apps/api/src/tenant/tenant.service.ts | 58 | Hardcoded DB role name `app_user` in `SET LOCAL ROLE`. Should be a constant (e.g., `APP_DB_ROLE`). |
| P2 | apps/api/src/tenant/tenant.service.ts | 60 | Hardcoded config key `app.tenant_id` in `set_config()`. Should be a constant (e.g., `TENANT_CONFIG_KEY`). |
| P2 | apps/api/src/user/user.service.ts | 136 | Hardcoded role string `'owner'` in ownership check. |
| P2 | apps/api/src/user/repositories/drizzleUser.repository.ts | 142 | Hardcoded role string `'owner'` in `getOwnedOrganizations` query. |
| P2 | apps/api/src/user/repositories/drizzleUser.repository.ts | 187 | Hardcoded role string `'owner'` in `transferOrgOwnership` update. |
| P2 | apps/api/src/user/repositories/drizzleUserPurge.repository.ts | 76 | Hardcoded role string `'owner'` in `purgeOwnedOrganizations` query. |
| P2 | apps/api/src/organization/organization.service.ts | 33 | Hardcoded role string `'owner'` in ownership check. |
| P2 | apps/api/src/organization/organization.service.ts | 61 | Hardcoded role string `'owner'` in ownership check. |
| P2 | apps/api/src/user/repositories/drizzleUser.repository.ts | 216 | Hardcoded invitation status `'expired'` in `expireOrgInvitations`. |
| P2 | apps/api/src/organization/repositories/drizzleOrg.repository.ts | 106 | Hardcoded invitation status `'expired'` in `expireOrgInvitations`. |
| P3 | apps/api/src/user/user.service.ts | 15 | Hardcoded in-memory cache TTL `60_000` ms (`SOFT_DELETE_CACHE_TTL_MS`). Should be configurable via env or `SystemSettings`. |
| P3 | apps/api/src/tenant/tenant.interceptor.ts | 35-38 | Hardcoded route whitelist `ORG_DELETED_ALLOWED_PATTERNS` for soft-deleted org access. Should be configurable or derived from metadata. |
| P3 | apps/api/src/user/repositories/drizzleUserPurge.repository.ts | 86 | Hardcoded `deleted-` prefix for anonymized organization slug. |

### Metrics
- files_audited: 30
- issues_found: 19
- P0: 0 | P1: 5 | P2: 11 | P3: 3

### Recommendations
| priority | effort | action |
|----------|--------|--------|
| 1 | S | Extract `OWNER_ROLE` constant into `apps/api/src/common/constants.ts` (or schema enum) and replace all 6 `'owner'` literals. |
| 2 | S | Extract `INVITATION_STATUS_EXPIRED` constant and replace both `'expired'` literals. |
| 3 | S | Extract `APP_DB_ROLE` and `TENANT_CONFIG_KEY` constants in `tenant.service.ts`. |
| 4 | M | Resolve TODO in `tenant.interceptor.ts` (Phase 3, #389): implement parent tenant resolution or throw when `parentOrganizationId` is present. |
| 5 | M | Replace silent fallbacks in `tenant.interceptor.ts` with `DatabaseUnavailableException` or `TenantContextMissingException` instead of returning `orgId`. |
| 6 | S | Make `better-auth.session_token` cookie name configurable via `SystemSettings` or auth config, or read it from the auth instance. |
| 7 | S | Make `anonymizedEmail` domain and `anonymizedSlug` prefix configurable. |
| 8 | M | Implement batch optimization in `drizzleUserPurge.repository.ts` (use `inArray()` + SQL-level random slug generation). |
| 9 | S | Move `SOFT_DELETE_CACHE_TTL_MS` to `SystemSettings` or env config. |
| 10 | S | Make `ORG_DELETED_ALLOWED_PATTERNS` configurable via decorator/metadata instead of regex list. |
