### Summary
Shared packages are mostly clean with 13 issues found across 153 files. Two UI components reference `React` types without importing the namespace (P1), three CLI locations hardcode `localhost:4000` (P2), and there are stale TODO/planning comments plus minor inconsistencies (P3).

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P1 | packages/ui/src/components/ConfirmDialog.tsx | 24 | Uses `React.ReactNode` without importing `React` (missing `import type * as React from 'react'`) |
| P1 | packages/ui/src/components/Skeleton.tsx | 3 | Uses `React.ComponentProps<'div'>` without importing `React` (missing `import type * as React from 'react'`) |
| P2 | packages/ui/src/components/ContextMenu.tsx | 23 | Stale TODO: `ContextMenuContent` is fully implemented but comment says `TODO: implement` |
| P2 | packages/ui/src/components/ContextMenu.tsx | 42 | Stale TODO: `ContextMenuItem` is fully implemented but comment says `TODO: implement` |
| P2 | packages/cli/src/commands/auth.ts | 5 | Hardcoded `DEFAULT_API_URL = 'http://localhost:4000'` — should be environment-driven or configurable |
| P2 | packages/cli/scripts/extractOpenapiSpec.ts | 19 | Hardcoded `http://localhost:4000` fallback — should respect environment or config |
| P2 | packages/cli/package.json | 12 | Hardcoded `http://localhost:4000` in `generate:api-types` npm script |
| P3 | packages/email/src/translations/index.ts | 6 | Duplicate type import: `import type { Translations } from './types'` is redundant because line 4 already exports it from `./types` |
| P3 | packages/types/src/shared/index.ts | 3 | Stale planning comment: `Slice 6 (#507) will add a lint rule to enforce import boundaries` — no evidence of active work |
| P3 | packages/types/src/shared/index.ts | 8 | Stale planning comment: `Slice 6 candidate: review if SENSITIVE_FIELDS becomes api-only` |
| P3 | packages/types/src/shared/index.ts | 10 | Stale planning comment: `Slice 6 candidate: review if DICEBEAR constants lose api consumer` |
| P3 | packages/types/src/shared/index.ts | 11 | Stale planning comment: `Slice 6 candidate: ConsentState/ConsentActions may be ui-only` |
| P3 | packages/ui/src/themes/presetsColorB.ts | 131 | Inconsistent color preset: `rose` dark mode overrides `sidebar` but light mode does not (no other color preset overrides `sidebar`) |

### Metrics
- files_audited: 153
- issues_found: 13
- P0: 0 | P1: 2 | P2: 5 | P3: 6

### Recommendations
1. **Fix missing React imports** (ConfirmDialog.tsx, Skeleton.tsx) — add `import type * as React from 'react'` or change to `import type { ReactNode, ComponentProps } from 'react'`. Effort: 5 min.
2. **Remove stale TODOs** (ContextMenu.tsx) — lines 23 and 42 are misleading since the components are fully implemented. Effort: 2 min.
3. **Extract CLI default API URL to config** — introduce a `ROXABI_API_URL` env var or a shared config default, and replace hardcoded `localhost:4000` in `auth.ts`, `extractOpenapiSpec.ts`, and `package.json`. Effort: 15 min.
4. **Clean up duplicate type import** (email/src/translations/index.ts) — remove redundant `import type { Translations }` on line 6. Effort: 2 min.
5. **Reconcile stale planning comments** (types/src/shared/index.ts) — verify whether Slice 6 is still planned; if yes, create a tracking issue and remove the inline comments. If no, delete the comments. Effort: 10 min.
6. **Audit `rose` color preset** (presetsColorB.ts) — determine if `sidebar` override in dark mode is intentional; if not, remove it for consistency. Effort: 5 min.
