### Summary
P4 (Consent, GDPR, audit, purge) is clean of TODO/FIXME/HACK/XXX, console.log, deprecated APIs, stale comments, and commented-out code. Four issues found: two clusters of hardcoded values in purge logic, one missing query-limit consistency in GDPR export, and one direct `process.env` read in a controller that already injects `ConfigService`.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P2 | `apps/api/src/purge/purge.service.ts` | 38, 67 | Magic number `100` used as batch size in both `purgeUsers` and `purgeOrganizations` without a named constant (e.g., `PURGE_BATCH_SIZE`). |
| P2 | `apps/api/src/purge/purge.service.ts` | 42, 72, 77, 83 | Hardcoded anonymization tokens: `@anonymized.local` (idempotency check), `deleted-` (slug prefix), `Deleted Organization` (name). These are coupled to `userPurgeService` internals and should be centralized constants. |
| P2 | `apps/api/src/gdpr/gdpr.service.ts` | 128, 157, 167 | `members` query in `fetchCoreUserData` and both `sentInvitations`/`receivedInvitations` queries in `fetchAndDeduplicateInvitations` lack `EXPORT_QUERY_LIMIT` (10_000), while sibling queries in the same methods enforce it. Potential unbounded result sets. |
| P3 | `apps/api/src/gdpr/gdpr.controller.ts` | 25 | Direct `process.env.APP_NAME` access instead of using the injected `ConfigService`. Inconsistent with the `configService` dependency already present in the constructor. |

### Metrics
- files_audited: 29
- issues_found: 4
- P0: 0 | P1: 0 | P2: 3 | P3: 1

### Recommendations (prioritized, with effort estimate)
1. **Extract purge constants** (~15 min) — Add `PURGE_BATCH_SIZE`, `ANONYMIZED_EMAIL_DOMAIN`, `ANONYMIZED_ORG_SLUG_PREFIX`, `ANONYMIZED_ORG_NAME` to `packages/config` or a local `purge.constants.ts`. Update `purge.service.ts` and tests.
2. **Add `EXPORT_QUERY_LIMIT` to GDPR invitation/org queries** (~10 min) — Append `.limit(EXPORT_QUERY_LIMIT)` to the `members` query (line 128) and both `invitations` queries (lines 157, 167) in `gdpr.service.ts`.
3. **Replace `process.env` with `ConfigService`** (~5 min) — In `gdpr.controller.ts`, change line 25 to `this.configService.get('APP_NAME') ?? 'App'`.
