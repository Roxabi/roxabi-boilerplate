### Summary
P3 partition (admin, system-settings, feature-flags) is relatively clean: no TODO/FIXME/HACK/XXX markers, no `console.log` leakage, no deprecated APIs, and no hardcoded localhost URLs. Five medium-severity issues were found: one unnamed PostgreSQL error code, one dead method with a stale backward-compatibility claim, and two duplicated magic numbers (org depth limit `3` and descendant threshold `1000`). Two low-severity issues complete the list: a generic `Error` throw in a repository and hardcoded UUIDs in a controller test.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P2 | apps/api/src/admin/adminUsers.service.ts | 239 | Magic string `'40001'` (PostgreSQL serialization_failure) used inline instead of a named constant. `PG_UNIQUE_VIOLATION` is already centralized in `pgErrorCodes.ts`; `40001` should follow the same pattern. |
| P2 | apps/api/src/admin/adminAuditLogs.service.ts | 120–124 | Dead code: `redactSensitiveFields` public instance method claims it is "Kept as public instance method for backward compatibility with existing callers", but no external callers exist. The standalone utility `redactSensitiveFields` from `utils/redactSensitiveFields.ts` is used instead. |
| P2 | apps/api/src/admin/adminOrganizations.service.ts | 155 | Magic number `3` for maximum organization hierarchy depth (`depth + 1 >= 3`). Not extracted as a named constant; duplicated in `adminOrganizations.hierarchy.ts:48`. |
| P2 | apps/api/src/admin/adminOrganizations.hierarchy.ts | 48 | Same magic number `3` for max depth (`depth + 1 + subtreeDepth >= 3`). Should be a shared constant (e.g., `MAX_ORG_DEPTH`). |
| P2 | apps/api/src/admin/adminOrganizations.query.ts | 53 | Magic number `1000` for tree-view availability threshold (`countResult?.count ?? 0) > 1000`). Duplicated in `adminOrganizations.hierarchy.ts:126` as a safety cap. |
| P2 | apps/api/src/admin/adminOrganizations.hierarchy.ts | 126 | Same magic number `1000` for descendant-org safety cap (`ids.length >= 1000`). Should be a shared constant (e.g., `MAX_DESCENDANT_ORG_IDS`). |
| P3 | apps/api/src/system-settings/repositories/drizzleSystemSettings.repository.ts | 19 | Generic `throw new Error('Unknown setting type in database: ...')` instead of a domain-specific exception. Repository errors should use typed exceptions so callers can distinguish invariant failures from runtime errors. |
| P3 | apps/api/src/admin/adminUsers.controller.test.ts | 100 | Hardcoded UUID `'00000000-0000-4000-8000-000000000001'` in test parameters. Test fixtures should ideally use a factory or helper to generate deterministic IDs. |
| P3 | apps/api/src/admin/adminUsers.controller.test.ts | 109 | Same hardcoded UUID repeated in the assertion block. |

### Metrics
- files_audited: 87
- issues_found: 9
- P0: 0 | P1: 0 | P2: 6 | P3: 3

### Recommendations
1. **Add `PG_SERIALIZATION_FAILURE` to `pgErrorCodes.ts`** and replace the inline `'40001'` in `adminUsers.service.ts`. Effort: 5 min.
2. **Remove dead `redactSensitiveFields` method** from `AdminAuditLogsService` and delete the stale comment. Effort: 2 min.
3. **Extract `MAX_ORG_DEPTH = 3`** and `MAX_TREE_VIEW_ORGS = 1000` into named constants in `adminOrganizations.hierarchy.ts` (or a shared `admin/constants.ts` file), then replace all inline occurrences. Effort: 10 min.
4. **Replace generic `Error` in `drizzleSystemSettings.repository.ts`** with a dedicated exception (e.g., `InternalSettingTypeError` or reuse `SettingValidationException`). Effort: 5 min.
5. **Introduce a test UUID factory** for `adminUsers.controller.test.ts` to eliminate hardcoded UUIDs. Effort: 10 min.
