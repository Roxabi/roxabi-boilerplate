### Summary
Package tests have a few assertion and teardown gaps; e2e suite lacks coverage on critical SaaS paths (billing, RBAC enforcement, tenant isolation) and contains conditional skips that can mask regressions.

### Findings
| severity | file | line | description |
|---|---|---|---|
| P1 | packages/cli/src/lib/credentials.test.ts | 87 | Test without assertion: `it('clearCredentials is a no-op when no file exists', () => { clearCredentials() })` never asserts; always passes silently |
| P1 | packages/ui/src/components/StatCounter.test.tsx | 21 | `Object.defineProperty(window, 'matchMedia', ...)` mutates global at module level; `afterEach` does not restore original `window.matchMedia` |
| P1 | packages/ui/src/components/StatCounter.test.tsx | 28 | `vi.stubGlobal('requestAnimationFrame', ...)` and `vi.stubGlobal('cancelAnimationFrame', ...)` at module level; `afterEach` lacks `vi.unstubAllGlobals()` so stubs leak across files |
| P1 | apps/web/e2e | — | Zero e2e coverage for billing (no billing.spec.ts or equivalent); critical path gap for a SaaS boilerplate |
| P1 | apps/web/e2e | — | Zero e2e coverage for RBAC enforcement (no tests verify that unauthorized users are blocked from admin/system-admin routes) |
| P1 | apps/web/e2e | — | Zero e2e coverage for tenant isolation (no cross-tenant data-leakage tests; e.g., Org A user seeing Org B members) |
| P2 | packages/ui/src/components/Tooltip.test.tsx | 5 | `beforeAll` sets `globalThis.ResizeObserver` but no `afterAll` restores it; leaks to subsequent test files |
| P2 | packages/ui/src/components/Slider.test.tsx | 5 | `beforeAll` sets `globalThis.ResizeObserver` but no `afterAll` restores it; leaks to subsequent test files |
| P2 | packages/cli/src/lib/output.test.ts | 5 | All 5 tests create `vi.spyOn(console, 'log')` inline and call `spy.mockRestore()` at end; if an assertion fails, the mock is never restored and leaks to the next test (no `afterEach` with `vi.restoreAllMocks()`) |
| P2 | apps/web/e2e/auth.spec.ts | 49 | `test.skip` permanently disables protected-route redirect test (TODO comment: "SSR renders the shell only; auth is enforced client-side") |
| P2 | apps/web/e2e/auth.spec.ts | 56 | OAuth button tests (Google, GitHub) use `if (isVisible) { await expect(...).toBeVisible() }` — passes silently when button is missing, masking UI regressions |
| P2 | apps/web/e2e | — | API keys are created and revoked in e2e, but no test verifies that a created key can actually authenticate an API request |
| P3 | apps/web/e2e/landing.spec.ts | 45 | `getStartedLink` href assertion uses `/.+/` regex which accepts any non-empty string; does not verify the actual docs URL |
| P3 | apps/web/e2e/registration.spec.ts | 50 | Test name says "should show error for duplicate email" but asserts success state (`backToLoginLink` visible); behavior is correct (privacy protection) but name is misleading |
| P3 | apps/web/e2e/admin.spec.ts | 73 | Conditional `test.skip()` when viewport < 768px can mask responsive OrgSwitcher regressions |
| P3 | apps/web/e2e/admin.spec.ts | 98 | Conditional `test.skip()` when only one org is present can mask org-switcher UI regressions |
| P3 | apps/web/e2e/admin.spec.ts | 113 | Conditional `test.skip()` when org name cannot be read can mask OrgSwitcher rendering regressions |
| P3 | apps/web/e2e/systemAdmin.spec.ts | 95 | Conditional `test.skip(true, 'Filter UI variant not found')` can mask FilterBar UI regressions |

### Metrics
- files_audited: 67
- issues_found: 17
- P0: 0 | P1: 6 | P2: 6 | P3: 5

### Recommendations (prioritized, with effort estimate)

1. **Add missing assertion** — `packages/cli/src/lib/credentials.test.ts:87` add `expect(clearCredentials()).toBeUndefined()` or `expect(() => clearCredentials()).not.toThrow()` (XS)
2. **Fix global teardown in StatCounter** — `packages/ui/src/components/StatCounter.test.tsx` add `afterEach(() => { vi.unstubAllGlobals(); window.matchMedia = originalMatchMedia })` (S)
3. **Fix global teardown in Tooltip/Slider** — add `afterAll(() => { delete globalThis.ResizeObserver })` to both files (XS)
4. **Fix spy leak in output tests** — `packages/cli/src/lib/output.test.ts` add `afterEach(() => vi.restoreAllMocks())` (XS)
5. **Fix OAuth conditional assertions** — `apps/web/e2e/auth.spec.ts:56` remove `if (isVisible)` guard; use `await expect(auth.googleOAuthButton).toBeVisible()` directly (XS)
6. **Unskip or implement SSR auth redirect** — `apps/web/e2e/auth.spec.ts:49` resolve the skipped test by implementing SSR-level auth enforcement or converting to a client-only assertion (M)
7. **Add billing e2e coverage** — create `billing.spec.ts` covering subscription flow, plan selection, and billing history (L)
8. **Add RBAC enforcement e2e** — create tests that verify a non-admin user receives a 403 or redirect when accessing `/admin/users` and `/admin/system-settings` (L)
9. **Add tenant isolation e2e** — create tests that verify a user in Org A cannot see Org B members, settings, or audit logs (L)
10. **Add API key authentication e2e** — extend `apiKeys.spec.ts` to use the created key in a `page.context().request` call to verify it authenticates (M)
11. **Replace conditional e2e skips with fixed assertions** — where possible (e.g., `systemAdmin.spec.ts:95`), remove `test.skip()` and use a deterministic page object or fail explicitly on missing UI (M)
