### Summary
Partition P8 (shared packages) is largely clean with respect to async patterns. One unhandled promise rejection risk was found in a CLI development script where `main()` is invoked without `await` or `.catch()`.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P2 | packages/cli/scripts/extractOpenapiSpec.ts | 36 | `main()` called without `await` or `.catch()`. If `fetch()` throws (e.g. ECONNREFUSED, DNS failure), the rejection is unhandled and exits with a non-descriptive crash instead of a user-friendly error message. |

### Metrics
- files_audited: 97
- issues_found: 1
- P0: 0 | P1: 0 | P2: 1 | P3: 0

### Recommendations (prioritized, with effort estimate)

1. **Wrap `main()` in `extractOpenapiSpec.ts` with try/catch or `.catch()`** — 5 min
   Change `main()` to `main().catch((err) => { console.error('Failed to fetch OpenAPI spec:', err.message); process.exit(1) })` so network errors produce a clear message and consistent exit code.

2. **(Clean) No action needed for:**
   - `packages/cli/src/lib/client.ts` — all async paths correctly propagate errors; `parseErrorBody` has its own try/catch.
   - `packages/cli/src/commands/*.ts` — all `await` calls are present; unhandled errors bubble up to `index.ts` which catches with `.catch()` and exits.
   - `packages/email/src/index.ts` — all `render()` calls are awaited.
   - `packages/ui/src/components/StatCounter.tsx` — `requestAnimationFrame` is properly cleaned up with `cancelAnimationFrame`.
   - `packages/ui/src/lib/useReducedMotion.ts` — `useSyncExternalStore` subscribe/cleanup is correct.
   - No Drizzle/db calls, no `.then()` without `.catch`, no async loops, no `Promise.all` issues, no `fs.*Sync` in hot paths.
