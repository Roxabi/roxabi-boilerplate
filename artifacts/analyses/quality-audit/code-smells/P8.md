### Summary
Partition P8 (shared packages) has several DRY violations in the email renderer and CLI commands, plus data duplication in theme presets. No critical security or correctness issues found.

### Findings

| severity | file | line | description |
|---|------|------|-------------|
| P1 | packages/email/src/index.ts | 29-48 | `renderVerificationEmail` — near-identical render boilerplate repeated 4× (renderResetEmail, renderMagicLinkEmail, renderExistingAccountEmail). Each does: destructure options, getTranslations, createElement, render html+text, return {html,text,subject}. |
| P1 | packages/email/src/templates/verification.tsx | 1-15 | `VerificationEmail` is a pure pass-through to `ActionEmail`. Same pattern in `magicLink.tsx`, `existingAccount.tsx`, `resetPassword.tsx` — 4 files, 4 identical proxy components. |
| P1 | packages/email/tests/magicLink.test.ts | 1-63 | Test file structure duplicated across `verification.test.ts` and `resetPassword.test.ts` — same 8 test cases (HTML CTA, text, locale, fallback, snapshots) with only strings changed. |
| P2 | packages/ui/src/themes/presetsColorA.ts | 12, 13, 25, 45, 46, 58, ... | `secondary: 'oklch(0.967 0.001 286.375)'` and `secondary-foreground: 'oklch(0.21 0.006 285.885)'` repeated in all 17 color presets (light mode). Dark mode `secondary: 'oklch(0.274 0.006 286.033)'` repeated 17×. Total ~51 repetitions of identical literals. |
| P2 | packages/ui/src/themes/presetsColorB.ts | 131 | `rose` dark preset defines `sidebar` (line 131) while no other color preset does. Inconsistent data shape — merge behavior differs for rose vs other colors. |
| P2 | packages/ui/src/themes/presetsHelpers.ts | 44-47, 77-84 | `getComposedConfig` re-implements the same `{...ZINC_BASE.light, ...base.cssVars.light, ...color?.cssVars.light}` merge logic already present in `getComposedDerivedTheme` (lines 22-36). |
| P2 | packages/email/src/index.ts | 20-27 | `escapeHtml` is exported but never used within the email package or consumers. Dead code. |
| P2 | packages/playwright-config/base.ts | 5, 6, 25, 26, 44, 65, ... | Hardcoded relative paths (`'./apps/web/e2e'`, `'./apps/web/e2e/.auth/user.json'`) with a comment warning that the config "must be consumed from the repo root". Fragile path coupling. |
| P2 | packages/ui/src/themes/presetsHelpers.ts | 78-79 | `getPresetConfig` does a runtime `BASE_PRESETS.find((p) => p.name === 'zinc')` lookup. If zinc is removed/renamed, this throws at runtime. `ZINC_BASE` is already statically imported. |
| P3 | packages/ui/src/components/ContextMenu.tsx | 23, 42 | TODO comments: "implement — style matching DropdownMenuContent" and "implement — style matching DropdownMenuItem". Incomplete implementation. |
| P3 | packages/ui/src/components/ConfirmDialog.tsx | 17 | `info` variant in `variantStyles` is an empty string `''`, meaning the confirm button has no visual style when variant='info'. |
| P3 | packages/cli/src/commands/members.ts | 22-56 | CLI list commands (`orgs.ts`, `roles.ts`, `users.ts`, `invitations.ts`, `members.ts`) share an identical pattern: local interface, `--json` option, createClient, fetch, printJson/printTable. 6 files, ~30 lines each of structural repetition. |

### Metrics
- files_audited: 67
- issues_found: 12
- P0: 0 | P1: 3 | P2: 6 | P3: 3

### Recommendations

| priority | recommendation | effort |
|---|---|---|
| 1 | Extract a single `renderEmail<T>(Template, url, locale, options)` helper in `packages/email/src/index.ts` to eliminate the 4 duplicated render functions. | S (1 file, ~15 lines) |
| 2 | Unify the 4 email template pass-through components into a single generic `ActionEmail` export, or use a factory function. | S (4 files → 1, ~10 lines) |
| 3 | Introduce `LIGHT_SECONDARY`, `DARK_SECONDARY`, `SECONDARY_FOREGROUND` constants in `presetsTypes.ts` and reference them in `presetsColorA.ts` / `presetsColorB.ts` to eliminate 51 literal repetitions. | S (2 files, ~20 lines) |
| 4 | Deduplicate email test files by parameterizing over `(renderer, localeStrings, url)` — one parameterized test suite instead of 3 identical files. | S (3 test files → 1) |
| 5 | Fix `rose` preset inconsistency: either add `sidebar` to all color presets or remove it from `rose`. | XS (1 line) |
| 6 | Replace `getPresetConfig` runtime zinc lookup with the statically-imported `ZINC_BASE`. | XS (1 line) |
| 7 | Delete unused `escapeHtml` export from `packages/email/src/index.ts`, or document why it is exported. | XS (1 line) |
| 8 | Resolve or remove ContextMenu TODO comments; implement the missing styles or delete the component. | M (1 file) |
| 9 | Add `info` variant style to `ConfirmDialog` or remove the variant from the type union. | XS (1 line) |
| 10 | Parameterize CLI list commands via a factory function `createListCommand(entity, endpoint, columns)` to reduce the 6-file repetition. | M (1 new file, refactor 6) |
| 11 | Consider resolving Playwright config paths dynamically (e.g. via `process.cwd()` or package-root resolution) instead of hardcoded `'./apps/web/...'`. | M (1 file) |
